get:
  tags:
    - Tarefas
  summary: Lista de todas as notas.
  responses:
    200:
      description: Lista completa de tarefas.
      schema:
        type: array
        items:
          $ref: "#/components/schemas/Note"
